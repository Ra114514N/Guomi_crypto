# 项目介绍 · 基于国密算法的安全数据传输与身份认证系统

> 本文件面向「重新设计前端样式」的协作者。读完后你应当能理解：这个项目是做什么的、有哪几个功能页面、每个页面展示什么数据、当前界面长什么样、以及前端定制可以从哪些接口入手。

---

## 一、项目是做什么的

这是一个**国密（GM/T，中国国家商用密码标准）安全数据传输演示系统**，大学综合实践课程设计。

核心场景：模拟「发送方 → 接收方」的一次安全通信。发送方把一份明文加密、签名、打包成一个**信封报文（envelope）**，接收方收到后逐步解封、验签、解密、校验，最终确认这份数据「来源可信、未被篡改、解密正确」。

整个系统的卖点是**把密码学的每一步中间过程都可视化展示出来**（这是课程演示的要求，见 `建议.md`），所以前端的核心任务是**清晰地呈现一条数据从明文到信封、再从信封还原成明文的完整链路**。

它不是生产系统，而是教学演示，所以追求的是**正确性 + 过程清晰**，而非性能或安全加固。

---

## 二、用到的国密算法（理解界面术语用）

| 算法 | 国标 | 在系统里干什么 | 类比（方便理解）|
|------|------|----------------|------------------|
| **SM2** | GB/T 35276 | 非对称加密，用接收方公钥「封装」会话密钥 | 类似 RSA |
| **SM3** | GB/T 32905 | 哈希算法，算摘要、做完整性校验 | 类似 SHA-256 |
| **SM4** | GB/T 32907 | 对称加密，真正加密数据（支持 CBC/CTR/GCM 三种模式）| 类似 AES |
| **ZUC-128** | GB/T 33133 | 流加密，另一条加密路径（祖冲之算法）| 类似 ChaCha20 |
| **SM9** | GB/T 38635 | 基于身份的签名，证明数据来自声称的发送方 | 身份即公钥的数字签名 |
| **HKDF-SM3** | — | 密钥派生，从一个 16 字节主密钥派生出 5 把子密钥 | 一粒种子长出多把钥匙 |

界面上频繁出现的名词：
- **会话秘密 / session secret**：发送方随机生成的 16 字节，是所有子密钥的源头。
- **信封 / envelope**：最终产物，一个 JSON 文件（`artifacts/message.json`）。
- **transcript（签名范围）**：`header || wrapped_secret || nonce || ciphertext || auth_tag`，SM9 对它整体签名。
- **认证标签 / auth_tag**：完整性凭证。GCM 模式下是 GCM 自带的 tag，其它模式是 HMAC-SM3 算出来的。

---

## 三、一次完整流程（界面要讲的故事）

这是界面要向观众讲清楚的**核心叙事**，前端的信息架构应该围绕这条线展开：

### 发送端（6 步）
1. **生成会话秘密** — `os.urandom(16)` 随机 16 字节，并生成 `session_id`。
2. **SM2 密钥封装** — 用接收方 SM2 公钥把会话秘密加密，得到 `wrapped_secret`。
3. **HKDF-SM3 密钥派生** — 用「上下文绑定」(`session_id|sender_id|receiver_id|suite_id`) 派生出 5 把子密钥：SM4 密钥(16B)、SM4-IV(16B)、ZUC 密钥(16B)、ZUC-IV(16B)、完整性密钥(32B)。
4. **对称加密** — 用 SM4（CBC/CTR/GCM）或 ZUC 加密明文，得到密文 `ciphertext`。
5. **完整性保护** — 非 GCM 模式用 HMAC-SM3 算出 `auth_tag`；GCM 模式直接用其内置认证 tag。
6. **SM9 签名** — 用发送方身份私钥对 transcript 签名，得到 `signature`。
7. 把以上全部打包写入 `artifacts/message.json`。

### 接收端（6 步，逆向验证）
1. **加载信封** — 读 `message.json`，识别算法、收发方身份。
2. **SM2 解封装** — 用接收方私钥还原 16 字节会话秘密。
3. **HKDF-SM3 派生** — 用相同上下文重新派生同样的 5 把子密钥。
4. **SM9 验签** — 重建 transcript，用发送方身份 + SM9 主公钥验证签名 → 证明**来源可信**。
5. **完整性验证** — 重算 HMAC / 验证 GCM tag → 证明**未被篡改**。
6. **解密 + 摘要比对** — 解密密文，重算明文 SM3 摘要与信封记录比对 → 证明**解密正确**。

最终三项全过才判定 `success = True`。界面用绿色✓ / 红色✗ 三个 badge 表达这三道关卡的结果。

### 信封报文实际结构（`artifacts/message.json`）
前端如果要直接渲染 JSON，字段如下：
```json
{
  "header": {
    "version": "3.0",
    "suite_id": "sm2-wrap+hkdf-sm3+sm4-gcm+sm9",
    "cipher": "sm4", "mode": "gcm", "kex_mode": "sm2_wrap",
    "sender_id": "sender@sm9.local", "receiver_id": "receiver@sm2.local",
    "session_id": "...", "timestamp": "...", "seq": 1,
    "integrity_algo": "hmac-sm3"
  },
  "algo_meta": { "algo": "SM4-GCM", "ciphertext_len": 943,
                 "plain_digest_hex": "...", "has_gcm_tag": true },
  "wrapped_secret_b64": "...",   // SM2 封装的会话秘密
  "nonce_or_iv_b64": "...",      // IV / Nonce
  "ciphertext_b64": "...",       // 密文
  "auth_tag_b64": "...",         // 认证标签
  "signature_b64": "..."         // SM9 签名
}
```

---

## 四、当前技术栈

- **GUI 框架**：**PySide6**（Qt for Python）。注意 git commit 写的是「更换了 pyqt6 界面」，但代码实际用的是 PySide6，不是 PyQt6 —— 两者 API 几乎一致，定制时按 PySide6 来。
- **后端逻辑**：纯 Python，分 `crypto/`（算法适配）和 `core/`（业务流程）两层。前端**不需要动后端**，只消费它返回的结果字典。
- **运行方式**：`python gui/run.py`（无构建系统，直接跑）。
- 旧的 `gui.py` 是早期 Tkinter 版本，已被 `gui/` 包取代，前端定制忽略它即可。

---

## 五、界面结构（这是定制的重点）

主窗口是**无边框窗口**（自绘标题栏），整体布局如下：

```
┌─────────────────────────────────────────────────────────┐
│ [◆ 品牌] 基于国密算法的安全数据传输…   [主题][−][□][×]   │  ← 自绘标题栏 (38px)
├──────────────────────────────┬──────────────────────────┤
│  功能区 (QTabWidget)          │   结果工作台              │
│  ┌────┬────┬────┬────┐       │   ┌────────────────────┐ │
│  │环境│演示│收发│性能│       │   │ 加密算法 [下拉]     │ │
│  └────┴────┴────┴────┘       │   │ 明文文件 [____][浏览]│ │
│                               │   │   [⚡ 执行]          │ │
│  （当前 tab 的内容区，        │   │ 最近结果 (ResultView)│ │
│    主要是 ResultView 富文本）  │   │ ● 状态  …  (busy dot)│ │
│                               │   └────────────────────┘ │
├──────────────────────────────┴──────────────────────────┤
│  日志输出                                       [清空]    │  ← 可拖动分隔条
│  [12:00:00] ▶ 系统就绪 …  (LogWidget, 带时间戳+配色)      │
└─────────────────────────────────────────────────────────┘
```

布局由 `QSplitter`（垂直）分成「上半区 + 日志区」，上半区再左右分成「功能区 tabs + 结果工作台」。

### 当前界面截图（默认主题·浅色）

![当前界面截图](docs_screenshot.png)

> 截图为默认蓝色主题、浅色模式下的初始状态（启动后显示就绪欢迎语）。标题栏右侧四个圆钮分别是「主题切换 / 最小化 / 最大化 / 关闭」。切换主题或深浅模式后，全部配色会实时刷新。

### 四个功能 Tab（`gui/tabs/`）

| Tab | 文件 | 作用 | 展示什么 |
|-----|------|------|----------|
| **环境** | `env_tab.py` | 检测运行环境 | Python 版本、平台、各算法组件可用状态（一堆 badge）、SM9 原生库是否加载、artifacts 已有文件 |
| **演示** | `demo_tab.py` | 一键跑完整流程 | 选算法+选文件→运行，把发送端+接收端全过程渲染成一长篇结构化报告 |
| **收发** | `send_recv_tab.py` | 分两步演示 | 「发送」按钮先展示发送端 6 步，「接收」按钮再展示接收端 6 步（分屏叙事感更强）|
| **性能** | `benchmark_tab.py` | 跑基准测试 | 1KB/64KB/1MB 三种数据量下各算法的加解密、签名、验签耗时表格 |

### 右侧「结果工作台」
独立于 tab，是一个**快速执行**入口：选算法 + 选文件 → 点「⚡执行」→ 在 `ResultView` 里显示执行概览、信封内容、三项验证结论。状态行有一个**会脉动的小圆点**(BusyDot) 和状态指示灯(StatusIndicator)。

### 日志区
`LogWidget`（`gui/log_widget.py`）——一个会**自动分类配色**的日志框：
- `▶` 开头 → 进度（强调色）；`✓` / 含「完成/通过」→ 绿色；`✗` / 含「失败/错误」→ 红色；`═══` → 大标题；`──` → 小节分隔。
- 自动加 `[HH:MM:SS]` 时间戳，对长 hex/base64 串用等宽+紫色高亮。

---

## 六、样式系统（前端定制从这里下手）

这是**最关键的一节**。当前界面的所有视觉都由两套机制控制：

### 1. 颜色主题：`config/style.json`
这是一个**纯数据文件**，定义了 5 套配色方案，每套都有 `light` / `dark` 两种模式：

- 已有主题：`默认`(蓝) / `深蓝` / `暖橙` / `墨绿` / `紫罗兰`
- 每套主题约 24 个颜色变量，含义见下表（节选）：

| 变量 | 含义 |
|------|------|
| `background_color` / `surface_color` | 窗口底色 / 卡片面色 |
| `text_color` / `text_muted` | 主文字 / 次要文字 |
| `accent_color` / `accent_hover` / `accent_soft` | 主强调色 / 悬停 / 浅强调底 |
| `success_color` / `error_color` / `warning_color` / `info_color` | 语义色（✓✗⚠•）|
| `mono_color` | 等宽串（hex/base64）的颜色 |
| `border_color` / `border_subtle` | 边框 / 弱边框 |
| `log_bg_color` / `log_text_color` | 日志区背景 / 文字 |
| `tab_selected_bg` / `tab_unselected_bg` | 选中/未选中 tab 底色 |

**改配色 = 改这个 JSON**，新增一套主题只要照格式加一个顶层 key。运行时切换主题的 UI 是标题栏的「主题」圆钮（`gui/theme_selector.py`）。

### 2. QSS 样式生成：`gui/styles.py`
- 启动时读取 `style.json`，`apply_color_scheme(主题名, 是否深色)` 把对应配色灌进一堆**全局变量**。
- `update_styles()` 用这些变量**拼接出 QSS 字符串**（`button_style` / `tab_style` / `group_style` / `log_style` 等），各组件再 `setStyleSheet()` 应用。
- **想改控件形态**（圆角、间距、字号、边框粗细）→ 改 `styles.py` 里对应的 QSS 模板。
- **想改配色** → 改 `style.json`，不用动 `styles.py`。

### 3. 结果内容的渲染：`gui/result_view.py`
四个 tab 的报告区不是普通文本，而是 `ResultView`（QTextEdit 子类），它提供一组**链式构建原语**，内部拼成 HTML 一次性渲染：

```python
view.section("发送端处理过程")     # 带强调色竖条的大标题
view.subsection("SM2 密钥封装")    # 【...】小标题
view.kv("协议版本", "3.0")          # 键值对
view.kv_mono("会话 ID", "...")      # 键值对（值等宽）
view.code_block("base64...")        # 带左边框的代码块
view.badge("签名验证", ok=True, detail="来源可信")  # 状态徽章
view.conclusion("全部验证通过", ok=True)  # 大结论框
view.hint("提示文字")               # 斜体次要提示
view.divider() / view.spacer()      # 分隔 / 留白
view.commit()                       # 一次性刷新
```

这些原语的 HTML/内联样式也都引用 `styles.py` 的颜色变量。**如果前端重做想换一套视觉语言**（比如卡片式、时间线式），主要就是重写 `result_view.py` 这些方法的 HTML 模板，或干脆把 `ResultView` 换成真正的 widget 组合。

### 4. 自绘动效组件：`gui/effects.py`
纯 QPainter 手绘的小组件，主题切换时会更新颜色：
- `BusyDot`：运行中脉动的圆点（正弦驱动）。
- `StatusIndicator`：idle/running/ok/error 四态状态灯。
- `BrandMark`：标题栏的菱形品牌标。
- `add_drop_shadow()` / `fade_in()`：投影、淡入动画辅助函数。

---

## 七、前端定制可能的切入点（给你参考）

按改动从小到大：

1. **只换配色**：改 `config/style.json`，或加新主题。零代码。
2. **调整控件外观**（圆角/间距/字体）：改 `gui/styles.py` 里的 QSS 模板。
3. **重做报告呈现方式**：改 `gui/result_view.py` 的原语 HTML（最能改变「过程展示」的观感）。
4. **改布局结构**（比如把左右分栏改成上下、把 tab 改成侧边导航）：改 `gui/main_window.py` 的 `_build_*` 方法。
5. **换动效/品牌元素**：改 `gui/effects.py`。
6. **彻底重写**：保留 `core/` 和 `crypto/`（后端逻辑别动），重写整个 `gui/` 包。后端给你的就是 `WorkflowWorker` / `BenchmarkWorker`（`gui/workers.py`）通过 Qt Signal 吐出来的结果字典和进度日志字符串。

### 前端与后端的契约（重写时只需认这些）
- **跑流程**：`gui/workers.py` 的 `WorkflowWorker(path, cipher, mode, sender_id)`，QThread，发三种信号：
  - `progress(str)`：实时日志行
  - `finished(dict)`：结果，结构 `{"send": {...}, "receive": {...}}`
  - `error(str)`：异常信息
- `send` 字典关键字段：`algo_label` / `plain_digest` / `auth_tag` / `signature_hex` / `output_dir` / `meta`(即完整 envelope)。
- `receive` 字典关键字段：`signature_ok` / `integrity_ok` / `digest_ok` / `success` / `plaintext_len` / `sender_id` / `receiver_id` / `algo_label` / `output_dir`。
- **跑性能测试**：`BenchmarkWorker`，`finished(str)` 给出 Markdown 表格文本。
- **环境检测**：`crypto.gmssl_loader.is_available()` / `error_message()`，同步调用。

只要新前端消费这套信号和字典，就能完全替换现有界面而不碰任何密码学逻辑。

---

## 八、运行与产物

- 启动 GUI：`python gui/run.py`
- 启动 CLI（另一套入口，无界面）：`python cli.py demo` / `send` / `benchmark` / `inspect-env`
- 所有产物写到 `artifacts/`：`message.json`(信封)、`recovered.txt`(还原明文)、`receiver_pri/pub.txt`(演示密钥)、`benchmark.md/csv`(性能报告)。

### 一个已知环境限制
SM9 依赖 GmSSL 原生动态库，且其主密钥对象在 Windows 上**不能稳定跨进程序列化**。所以演示走的是**同进程**流程（发送和接收在一次运行里完成，主密钥保存在内存）。这不影响前端，但解释了为什么没有「独立的接收端程序」。

---

## 附：目录速查

```
core/          业务逻辑（别动）
  protocol.py    协议常量（版本=3.0、套件 ID 定义）
  sender.py      发送端 6 步
  receiver.py    接收端 6 步验证
  workflow.py    一键完整流程
  benchmark.py   性能测试
crypto/        算法适配（别动）
gui/           ← 前端在这里
  run.py         入口
  main_window.py 主窗口 + 布局 + 标题栏
  styles.py      QSS 样式生成（改控件外观）
  result_view.py 结果富文本原语（改过程展示观感）
  log_widget.py  日志框（自动分类配色）
  effects.py     自绘动效组件
  theme_selector.py 主题切换弹窗
  tabs/          四个功能页
config/
  style.json     ← 配色主题数据（改配色）
artifacts/       运行产物
gui.py           旧 Tkinter 版（已弃用，忽略）
```
