### 一、 浅色主题字体双轨制 (Light Mode Typography)

在亮色背景下，光线投射更强，因此字体需要**适当加粗字重**以防止笔画被白色背景“吃掉”。

#### 1. 界面与文本字体 (UI & Sans-serif)

- **推荐：** `Inter` (英文/数字) + `Microsoft YaHei UI` / `PingFang SC` (中文)。
- **字号与字重配置：**
  - **步骤大标题：** `16px` / `Font-Weight: 650 (SemiBold)` / 颜色：深邃黑 `#111827`。
  - **普通标签/正文：** `14px` (比常规的12px明显放大) / `Font-Weight: 400` / 颜色：钢铁灰 `#374151`。
  - **次要辅助标签（Key）：** `12px` / `Font-Weight: 500` / 颜色：烟熏灰 `#6B7280`。

#### 2. 密码学数据流字体 (Data & Monospace)

在浅色背景下，长串的密文、Hash值如果使用纯黑，会像密密麻麻的蚂蚁，极难阅读。

- **推荐：** `JetBrains Mono` 或 `SF Mono`。
- **字号与字重配置：** `13px` / `Font-Weight: 550 (Medium)`。
- **关键色彩：** 使用**深沉、高对比度但不刺眼的莫兰迪代码色**，如深青色 (`#08747A`) 或深邃紫 (`#634DBF`)。

### 二、 浅色主题去容器化与高对比度网格 (Grid & Palette)

在浅色模式下，**绝对不要**使用大面积的深色方块去包裹文字。应当利用**极轻微的背景色差（Off-white）和精致的渐变**来引导视线。

```
┌────────────────────────────────────────────────────────┐
 │  ✓  1. 生成 SM9 主密钥对                              │ <- 纯白卡片 (#FFFFFF)
 │  ────────────────────────────────────────────────────  │ <- 极浅分割线 (#E5E7EB)
 │                                                        │
 │  用途                        算法                       │ <- 辅助标签：烟熏灰 (#6B7280, 12px)
 │  身份签名 / 验签             SM9 (GB/T 38635)           │ <- 核心文本：深邃黑 (#111827, 14px)
 │                                                        │
 │  公钥                                                  │
 │  ┌──────────────────────────────────────────────────┐  │
 │  │ 049783bbe3ccb71f4396dc81f3a9c554577e1c4711da...  │  │ <- 数据容器：微灰底 (#F8FAFC)
 │  └──────────────────────────────────────────────────┘  │    文字：高对比度深青色 (#08747A, 13px Mono)
 └────────────────────────────────────────────────────────┘
```

#### 🎨 浅色专属样式 Token (Design Tokens)

- **软件主背景 (App Bg)：** `#F4F6F8` (柔和的浅浅灰，避免纯白 `#FFFFFF` 全屏造成的刺眼感)。
- **卡片背景 (Surface Bg)：** `#FFFFFF` (纯白卡片，在浅灰底色上自然浮现，形成高级层级)。
- **极细分割线 (Border)：** `#E5E7EB` (极淡的灰色边框)。
- **数据包裹底色 (Mono Bg)：** `#F8FAFC` 或 `#F1F5F9` (带有一点点冷色调的微灰，让等宽数据沉下去)。

### 三、 步骤 5：浅色“安全验证胶囊”升级

在亮色模式下，红绿对比如果饱和度过高会显得极其俗气。商业级设计会采用**低饱和、高明度底色 ＋ 高对比度文字**的“胶囊徽章（Capsule Badge）”：

- **安全验证通过（Success Badge）：**
  - 背景色（Bg）：`rgba(16, 185, 129, 0.1)` (极淡的果汁绿)
  - 边框色（Border）：`#A7F3D0`
  - 文字颜色（Text）：`#065F46` (高对比度的深森林绿，确保字号大且极其清晰)
- **安全验证失败（Error Badge）：**
  - 背景色（Bg）：`rgba(239, 68, 68, 0.08)` (极淡的胭脂红)
  - 边框色（Border）：`#FCA5A5`
  - 文字颜色（Text）：`#991B1B` (深酒红)

### 四、 浅色主题主成果 QSS 样式表代码

直接输出为可覆写到你 `gui/styles.py` 中的浅色 QSS 代码段。这套样式表专门针对**大字号、高易读性、现代化 SaaS 质感**进行了像素级调校：

```
/* 整个系统的浅色主背景 */
QWidget#MainWindow {
    background-color: #F4F6F8;
}

/* 演示核心卡片：纯白、大内边距、微弱外投影 */
QFrame#StepCard {
    background-color: #FFFFFF;
    border: 1px solid #E5E7EB;
    border-radius: 8px;
    padding: 24px; /* 增大间距，配合大字号，形成呼吸感 */
}

/* 步骤大标题：字号放大至16px，纯黑加粗 */
QLabel#StepTitle {
    color: #111827;
    font-size: 16px;
    font-weight: 650;
    font-family: "Segoe UI", "Microsoft YaHei UI", sans-serif;
}

/* 错落网格的次要标签（Key）：烟熏灰 */
QLabel#DataLabel {
    color: #6B7280;
    font-size: 12px;
    font-weight: 500;
    margin-bottom: 4px;
}

/* 常规短数据（Value）：放大至14px，深灰黑，极易观看 */
QLabel#DataValueNormal {
    color: #1F2937;
    font-size: 14px;
    font-weight: 500;
}

/* 密码学 Mono 数据容器：深青高对比度，局部微灰底色包裹 */
QLabel#DataValueMono {
    color: #08747A; /* 殿堂级代码编辑器深青高亮色 */
    font-family: "JetBrains Mono", "Consolas", monospace;
    font-size: 13px; /* 放大 Mono 字体 */
    font-weight: 600;
    background-color: #F1F5F9; /* 优雅的微蓝灰底色 */
    border: 1px solid #E2E8F0;
    border-radius: 6px;
    padding: 4px 10px; /* 增大内边距，让长串数据非常易读 */
}

/* 浅色日志控制台：模拟高级纸张终端（Paper Console） */
QTextEdit#LogConsole {
    background-color: #F8FAFC;
    border: 1px solid #E2E8F0;
    border-radius: 6px;
    color: #334155;
    font-family: "JetBrains Mono", monospace;
    font-size: 12px;
}
```

### 五、 右侧日志流的浅色优雅高亮机制

在浅色模式下，右侧日志不能再使用暗色模式的荧光色。

- **时间戳 `[16:53:17]`：** 使用淡灰色 `#94A3B8`。
- **警告/攻击模拟 `[WARNING]`：** 使用深琥珀色文字 `#B45309` 搭配淡黄底色。
- **验证成功 `✓`：** 使用鲜明的翠绿色 `#15803D`。